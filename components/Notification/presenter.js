import React from "react";
import PropTypes from "prop-types";
import { View, Text, TouchableOpacity } from "react-native";

const Notification = props => <View />;

Notification.propTypes = {};

export default Notification;
